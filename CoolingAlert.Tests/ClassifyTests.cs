using Xunit;

namespace CoolingAlert.Tests;

// ----------- STEP 5 -----------------
/*
public class ClassifyTests
{
    [Fact]
    public void BatteryOk()
    {
        var batteryData = new BatteryDataModel { BatteryId = 10567, ThermalManagementType = ThermalManagementType.THERMAL_PASSIVE, Temperature = 25 };
        var state = Classify.ClassifyBatteryState(batteryData);
        Assert.Equal(BatteryState.BATTERY_OK, state);
    }

    [Fact]
    public void BatteryAlert()
    {
        var batteryData = new BatteryDataModel { BatteryId = 21389, ThermalManagementType = ThermalManagementType.THERMAL_HYBRID, Temperature = 60 };
        var state = Classify.ClassifyBatteryState(batteryData);
        Assert.Equal(BatteryState.BATTERY_ALERT, state);
    }
}
*/
// ----------- STEP 5 code ends -------
